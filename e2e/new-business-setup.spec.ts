import { test } from "@playwright/test";

test("owner sets up a new business and publishes its storefront", () => {
  test.fixme(true, "Requires onboarding/signup APIs and a real dashboard /onboarding setup UI.");
});