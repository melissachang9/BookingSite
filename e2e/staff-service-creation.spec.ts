import { test } from "@playwright/test";

test("manager creates and publishes a tenant-scoped service", () => {
  test.fixme(true, "Requires authenticated service creation routes and a real dashboard /services add-service UI.");
});